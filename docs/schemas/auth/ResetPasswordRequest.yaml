type: object
description: Request body for POST /api/auth/reset-password
required:
  - token
  - newPassword
properties:
  token:
    type: string
    minLength: 1
    example: abc123reset-token
  newPassword:
    type: string
    format: password
    minLength: 8
    example: NewSecurePass1
